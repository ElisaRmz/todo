<h1>Tus tareas</h1>

<%= link_to 'Añadir', new_task_path %>

<p>Ver tareas, hacer</p>

<table>

  <tr>
    <th><%= 'Completado' %></th>
    <th><%= 'Tarea' %></th>
    <th><%= 'Notas' %></th>
  </tr>

  <% @tasks&.each do |task| %>
    <tr>
      <td><%= task.finish %></td>
      <td><%= task.name %></td>
      <td><%= task.note %></td> 
    </tr> 
  <% end %>

</table>